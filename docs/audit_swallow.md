# PharmaSupplyBot Swallowed-Exception Audit

Generated by `scripts/audit_swallow.py`.

A *swallowed* exception is an `except Exception` block that neither
calls `logger.error(...)` / `logger.warning(...)` nor `raise`s.
Such handlers silently hide failures from the operator.

## Summary

| Metric | Count |
|--------|------:|
| Swallowed `except Exception` handlers in `src/` | 1 |
| Files in allowlist (intentional) | 5 |

## Per-file breakdown

| File | Count |
|------|------:|
| `src\core\database\order_runs_read.py` | 1 |

## All occurrences

| File:Line | Function | Snippet |
|-----------|----------|---------|
| `src\core\database\order_runs_read.py:98` | `database_is_ready` | `except Exception:` |

## Allowlist

These files are explicitly excluded — broad `except` is reviewed and intentional.

| File | Reason |
|------|--------|
| `src/ui/views/streamlit_process.py` | streamlit subprocess wrapper — UI-facing failure capture |
| `src/cli/logging_setup.py` | logging handler teardown — defensive, no recovery possible during interpreter shutdown |
| `src/ui/order/streamlit_order_process.py` | streamlit output-file close — best-effort teardown |
| `src/ui/streamlit_remove_cart.py` | streamlit output-file close — best-effort teardown |
| `src/ui/views/streamlit_product_matching.py` | streamlit output-file close — best-effort teardown |
