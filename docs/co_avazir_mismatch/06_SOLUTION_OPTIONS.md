# 6 — الحلول الممكنة + scoring

## معايير تقييم الحل

| معيار | وزن |
|-------|-----|
| يصلح حالة 80838 | 30 |
| لا regression | 25 |
| بساطة جراحية (surgical) | 20 |
| يغطي عائلات مشابهة (CO+brand) | 15 |
| لا يعتمد على بيانات خارجية/مخزون | 10 |
| **Total** | **/100** |

## S1 — رفض بادئة CO كعلامة مختلفة (مُنفَّذ)  
ملف: `tests/solutions/test_s1_reject_co_prefix_brand.py`

```python
def _co_prefixed_brand_mismatch(left, right) -> bool:
    longer, shorter = ...
    return longer == f"CO{shorter}"
```

| معيار | درجة |
|-------|------|
| يصلح 80838 | 30 |
| لا regression | 25 |
| بساطة جراحية | 20 |
| عائلات CO* | 15 |
| مستقل عن المخزون | 10 |
| **Total** | **100** |

**الأفضل.**

## S2 — رفع عتبة containment لـ len_diff==2 من 82 إلى 86 (مُنفَّذ كدفاع ثانٍ)

| معيار | درجة |
|-------|------|
| يصلح 80838 (ratio 85.7) | 30 |
| لا regression | 20 (أوسع قليلاً) |
| بساطة | 18 |
| عائلات CO* | 8 (عرضي) |
| مستقل عن المخزون | 10 |
| **Total** | **86** |

مفيد كشبكة أمان، لكنه أقل تحديداً من S1.

## S3 — الاعتماد على manual review / توفر المخزون فقط

| معيار | درجة |
|-------|------|
| يصلح 80838 | 10 (جزئي/ظرفي) |
| لا regression | 15 |
| بساطة | 10 |
| عائلات CO* | 0 |
| مستقل عن المخزون | 0 |
| **Total** | **35** |

**غير كافٍ** كحل جذري: لو AVAZIR orderable والصحيح غير orderable يعود الخطأ.

## S4 — تفعيل reject_extra_brand_token عالمياً

لا يحل الاتجاه العكسي (token ناقص في المرشح: `CO` موجود في الاستعلام لا في AVAZIR).
`has_extra_brand_token` يفحص tokens **زائدة في المرشح**.
**Score ≈ 25** — لا يناسب هذه الحالة.

## S5 — hardcode pair (CO_AVAZIR, AVAZIR)

يحل حالة واحدة فقط؛ هش.
**Score ≈ 40**.

## القرار

تنفيذ **S1 + S2 معاً** في `_brand_match_check`:
1. رفض صريح لـ `CO{brand}` vs `{brand}`
2. تشديد عتبة containment لفرق حرفين إلى `< 86`
